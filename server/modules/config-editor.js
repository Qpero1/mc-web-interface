/**
 * server/modules/config-editor.js
 * --------------------------------------------------------------------------
 * DEPRECATED — replaced by server/services/* + server/adapters/*.
 *
 * This file is no longer loaded by server/index.js. Domain logic for "config-editor"
 * lives in server/services/config-editorService.js (or the canonical service name).
 * Kept here only because the Windows file mount won't allow deletion from
 * this session — safe to delete manually.
 * --------------------------------------------------------------------------
 */
