# Copy this file to .env. .env is gitignored.

# Optional - override jwtSecret from config.json
JWT_SECRET=change_me_to_a_long_random_string

# Optional - override host/port
PORT=8787
HOST=0.0.0.0

# Set to "production" to serve the prebuilt client from client/dist
NODE_ENV=development
